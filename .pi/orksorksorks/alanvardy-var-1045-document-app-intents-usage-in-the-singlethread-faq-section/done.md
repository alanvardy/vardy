# Done

- **What was built**: Documented SingleThread's three App Intents (Apple Shortcuts) in the SingleThread page FAQ — a new "Shortcuts" `FaqCategory` in `src/interfaces/handlers/singlethread/web.rs` with 7 entries covering What's Next / Complete Current Task / Skip Current Task (what each does, Siri phrases, short titles), the four surfaces (Siri on a physical device, Shortcuts app search, Home Screen long-press, Spotlight), the grant-Reminders-access-first note, and all five edge-case response messages verbatim. Added one targeted test `faq_app_intents_documented` (rendered-page spot-check); the cascading FAQ tests cover the rest automatically. No template/schema/route/CSS-class changes needed.
- **Commit SHA(s)**:
  - `80fa85a` docs: document App Intents (Apple Shortcuts) in the SingleThread FAQ
  - `d8d462b` chore: regenerate CSS (FAQ copy introduces the visible utility)
  - `7e9d624` refactor: drop needless borrow in faq_app_intents_documented (clippy)
  - (plus DELETEME removal and artifact commits)
- **Verification**: `./scripts/test.sh` full gate green — 113/113 tests passed (fmt, sqlx prepare, check, CSS-drift, clippy `-D warnings`, nextest, TODO grep). FAQ tests including `faq_all_questions_appear`, `faq_all_answers_appear`, `faq_items_no_duplicate_questions`, `faq_items_grouped_under_category_headings`, `faq_summary_has_chevron`, and the new `faq_app_intents_documented` all pass.
- **Reviewer findings**: No blockers (merge OK). Two nits deferred as non-essential: (1) the new heading/question assertions overlap the cascade tests partly — the spec explicitly endorses one targeted assertion; (2) the answer-substring assertion has no escape-sensitive chars, so it would not catch an escaping regression (the question assertion does exercise escaping). One gate-caught convention fix was made post-review: clippy `needless_borrows_for_generic_args` on `body.contains(&heading)` → `body.contains(heading)` (web.rs:369), re-verified through the full gate.
- **Remaining manual items**: Optional only — screenshots of the app-icon long-press menu and the Shortcuts app listing (small.md marks these optional). `static/site.css` regeneration is committed in the same change (Tailwind emits a `.visible` utility because the FAQ copy contains the word "visible"); the fresh-worktree `test.db` was created locally via `cargo sqlx database create && cargo sqlx migrate run`. PR open/merge left to the user.