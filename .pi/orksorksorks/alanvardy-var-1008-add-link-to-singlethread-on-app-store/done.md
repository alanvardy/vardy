# Done

- **What was built**: Added an App Store download badge linking to `https://apps.apple.com/ca/app/singlethread/id6802537541`, rendered twice on the SingleThread page — in the hero below the platform badges (`templates/singlethread.html:28-33`) and at the closing CTA (`templates/singlethread.html:110-115`) — following the existing `home.html` external-link-with-logo pattern (`target="_blank" rel="noopener noreferrer"` + `asset_url` img). No Rust handler/route changes; `ROUTES.md` gained a one-line doc mention. The badge is the **official Apple white badge** from Apple's marketing toolbox (`badge-1/en-us.svg`, `Download_on_the_App_Store_Badge_US-UK_RGB_wht_092917`), served at the official 119.66×40 aspect — white variant chosen because the site has a dark background.
- **Commit SHA(s)**: `2fee99e` (initial badge) + `6404195` (official Apple badge swap) + `416e217` (enlarged to 180×60), pushed to `alanvardy-var-1008-add-link-to-singlethread-on-app-store`.
- **Verification**: `./scripts/test.sh` gate fully green — fmt, sqlx prepare (needed a freshly created `test.db` + migrated), check, CSS-drift check (no new Tailwind classes → `static/site.css` untouched), clippy `-D warnings` clean, `nextest` 112/112 passed (incl. extended `singlethread::web::tests::index_serves_ok_html` with 3 new assertions), no forgotten TODOs.
- **Reviewer findings**: No blockers, no P1s. P2 notes only: (1) `body.matches(...)` API used for count==2 assertions — confirmed by the green gate (`str::matches` is literal substring matching); (2) `rel="noopener noreferrer"` not asserted — small.md asked only for `target="_blank"`, deferred; (3) test comment explains static-text non-escaping instead of asserting escaped form — correct, deferred.
- **Remaining manual items**: The implement pass was completed directly in the session after the worker subagent timed out (it hung on a decorative SVG→PNG visual check after creating `static/app-store.svg`; its SVG was also rewritten to fix glyph/text overlap). The badge itself is now Apple's official toolbox SVG (verbatim, no modification). Visual QA of the badge rendering on a real device/browser is recommended before merge if the user wants to eyeball sizing (120×40 rendered). Pre-existing unrelated working-tree entries (` D DELETEME`, untracked `.pi/`) were left untouched. PR to merge with `--rebase`.