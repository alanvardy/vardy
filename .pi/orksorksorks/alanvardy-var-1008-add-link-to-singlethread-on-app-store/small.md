# Task

Add a link to the SingleThread app on the Apple App Store, with an App Store logo, at the **top and bottom** of the SingleThread page.

- Target URL: `https://apps.apple.com/ca/app/singlethread/id6802537541`
- Render an App Store badge (logo + link) in `templates/singlethread.html`, once in the hero area near the top (after the icon/badges, ~line 24) and once near the bottom CTA (~line 115). Both spots are inside the page's own `content` block — do not touch `templates/layout.html`.
- Follow the existing external-link-with-logo pattern in `templates/home.html:23-34` (GitHub/LinkedIn links: `<a href="…" target="_blank" rel="noopener noreferrer">` + `<img src="{{ asset_url('github.svg') }}">`), and the new footer link pattern seen in `templates/layout.html:19` (Unsplash credit) where applicable.
- Add a new badge asset `static/app-store.svg` (downloaded or hand-written official-style Apple App Store badge). `asset_url` in `src/app/assets.rs` picks up new files in `static/` automatically; no handler/route change is needed — the static URL can live in the template.
- If any new Tailwind utility classes are used, regenerate `static/site.css` via `scripts/build-css.sh` and commit it in the SAME change (`scripts/test.sh` enforces a CSS-drift check).
- No Rust code changes expected (`src/interfaces/handlers/singlethread/web.rs` untouched unless a context value is genuinely needed).

Tests: extend the inline `#[cfg(test)] mod tests` in `src/interfaces/handlers/singlethread/web.rs` (`index_serves_ok_html`, `web.rs:152-209`) with ~2–3 assertions for the new link(s) — the apps.apple.com URL at top and bottom, and `target="_blank"`. Remember minijinja autoescape: `/` renders as `&#x2f;` in hrefs (existing pattern at `web.rs:185`), so assert short unique substrings accordingly.

Also update `ROUTES.md` if the template change is reflected there (doc-only; routes themselves do not change).

Run `./scripts/test.sh` as the gate before committing. Branch protection: PR to `main`, merge with `--rebase` only.

## Why SMALL

Single-page template/static-only change (~4 files, 0 Rust code changes) following the existing home.html link-with-logo pattern; no schema, no new subsystem, no shared-layout risk, no design sign-off; a few local test assertions. All SMALL criteria A–F hold.

## Key files (locator recon)

- `templates/singlethread.html` — the only page to change (top ~line 24, bottom ~line 115)
- `static/app-store.svg` — new badge asset (auto-served; pattern: `static/github.svg` used via `asset_url` in `templates/home.html:23-34`)
- `static/site.css` — only if new Tailwind classes are used; must be regenerated by `scripts/build-css.sh` and committed together
- `src/interfaces/handlers/singlethread/web.rs` — inline tests (`index_serves_ok_html`, web.rs:152-209) get ~2–3 new assertions; handler itself unchanged
- `ROUTES.md` — doc-only touch, if any
- Note: `grep` confirms no existing apps.apple.com / App Store badge anywhere; `templates/home.html:23-34` is the pattern to copy; `.badge` span in `singlethread.html:18-23` is an existing element, not an app-store badge.