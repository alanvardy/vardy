# Task

VAR-727 — Mobile responsive changes. The site needs to render correctly on
mobile viewports: the contact form should span the full available width on
mobile (today it is squeezed into the right half of a two-column layout that
does not collapse wide enough), and the wallpaper background image plus its
Unsplash artist accreditation should be hidden on mobile devices (today they
render unconditionally on every viewport). This is a presentation-layer-only
change; page content and behavior are unchanged.